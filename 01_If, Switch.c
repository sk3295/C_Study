#include <stdio.h>

void main() {

	// if 
	// > 만약에 ~라면

	int hp = 100;

	if (hp > 80) {
		printf("...");

		if (hp >= 100) {
			printf("....");
		}
	}

	int ItemID = 0;

	switch (ItemID) {
	case 46: printf("당신에게 TNT가 주어졌습니다.");
		break;
	case 147: printf("당신에게 금 입력판이 주어졌습니다.");
		break;
	default:
		prntf("등록되지 않은 아이템 코드입니다.");
	}

	int loopTime = 0;
SavePoint:






}